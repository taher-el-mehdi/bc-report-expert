<UserControl x:Class="ReportExpert.Modules.Localization.Views.LocalizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12,8" BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
            <StackPanel Orientation="Horizontal">
                <ui:SymbolIcon Symbol="LocalLanguage24" Margin="0,0,8,0" />
                <TextBlock Text="Localization" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                <TextBlock Text=" — labels &amp; captions" Margin="8,0,0,0" Opacity="0.6" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Padding="12,8" BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding ReportPath, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas" />
                <ui:Button Grid.Column="1" Margin="8,0,0,0" Content="Browse" Command="{Binding BrowseReportCommand}" />
                <TextBlock Grid.Column="2" Text="Language" VerticalAlignment="Center" Margin="16,0,8,0" />
                <ComboBox Grid.Column="3" Width="140"
                          ItemsSource="{Binding Languages}"
                          SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" />
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" Margin="12,8"
                  ItemsSource="{Binding Labels}"
                  SelectedItem="{Binding SelectedLabel, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  BorderThickness="0" Background="Transparent">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Label name" Binding="{Binding LabelName, UpdateSourceTrigger=PropertyChanged}" Width="200" />
                <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*" />
                <DataGridTextColumn Header="Language" Binding="{Binding Language}" Width="100" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Padding="12,8" BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" FontSize="12" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:Button Content="Add" Command="{Binding AddLabelCommand}" Margin="0,0,6,0" />
                    <ui:Button Content="Remove" Command="{Binding RemoveLabelCommand}" Margin="0,0,6,0" />
                    <ui:Button Content="Export CSV" Command="{Binding ExportTranslationCommand}" Margin="0,0,6,0" />
                    <ui:Button Content="Import CSV" Command="{Binding ImportTranslationCommand}" Margin="0,0,6,0" />
                    <ui:Button Appearance="Primary" Content="Save labels" Command="{Binding SaveLabelsCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
